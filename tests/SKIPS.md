# Test skips ledger

Per Spec 13: "No test is skipped silently; if a test must be excluded, the
reason is recorded in `tests/SKIPS.md` with an issue link and expiry date."

| Test | Reason | Issue | Expiry |
|------|--------|-------|--------|
| _none yet_ | | | |
